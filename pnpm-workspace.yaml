minimumReleaseAgeExclude:
  - '@next/*'
  - next

# pnpm 10 blocks dependency build scripts by default. Approve core-js
# (transitive dep of jspdf — its postinstall only prints a funding notice).
onlyBuiltDependencies:
  - core-js
